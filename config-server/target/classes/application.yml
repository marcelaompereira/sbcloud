# files com profile diferente fornecedor-dev, fornecedor-prod
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marcelaompereira/microservices-repo
          search-paths: fornecedor